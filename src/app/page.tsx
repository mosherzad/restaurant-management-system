import HomeContent from "@/components/HomeContent";

export default function Home() {
  return (
    <div className="relative no-scrollbar w-full min-w-0 overflow-x-hidden lg:pr-[calc(360px+2rem)]">
      <HomeContent />
    </div>
  );
}
